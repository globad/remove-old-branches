{
  "name": "remove-old-branches",
  "version": "1.0.0",
  "description": "Removes old branches in Butbucket repository",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "globad",
  "license": "MIT",
  "dependencies": {
    "selenium-webdriver": "^4.0.0-alpha.1"
  }
}
